{
  "name": "keystorage",
  "version": "1.0.0",
  "workspaces": [
    "app/",
    "server/"
  ],
  "description": "A Key Storage App based on Redis.",
  "scripts": {
    "app":   "concurrently 'cd ./server && npm run server' 'cd ./app && npm run app'",
    "build:uncached": "docker compose build --no-cache",
    "build": "docker compose build",
    "start": "docker compose up",
    "start:detached": "docker compose up -d" 
  },
  "keywords": [],
  "author": "Gabriel Vinícius da Cruz",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
